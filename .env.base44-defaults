# Local defaults so the app boots before any real secrets are supplied.
# Real secrets from the user's dashboard (delivered via /run/base44/app.env)
# always override these values.

DATABASE_URL=postgresql://postgres:postgres@postgres:5432/digital_passport
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=base44-dev-secret-change-me-in-production-9f3a7c2e
NEXT_PUBLIC_SOCKET_URL=http://localhost:3000
NODE_ENV=development
